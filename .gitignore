# Secrets (never commit)
# Snowflake: use repo-root `.env` only (copy from `.env.example`).
# Streamlit: python-dotenv. dbt: env_var() in dbt/profiles.yml.
.env
.env.local
*.pem

# Local / personal
run.txt
Run.txt

# Python
venv/
.venv/
__pycache__/
*.py[cod]
*$py.class
.Python
*.so
.pytest_cache/
.mypy_cache/
.ruff_cache/

# dbt
dbt/target/
dbt/dbt_packages/
dbt/logs/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
